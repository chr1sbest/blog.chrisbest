h2  
    strong Adventures of a Self-Taught Developer
p
    ul
        each article, slug in public.articles.cs._data
            li
                if article.published!==false
                    a(href="/articles/cs/#{ slug }") #{ article.title }
                    span.date   | #{ article.date }
                    p(style="font-style: italic") #{ article.description }
                else
                    p(style="text-decoration: underline") #{ article.title }  | #{ article.date }
                    p(style="font-style: italic") #{ article.description }


br
h2 
    strong Topics in Psychology
p
    ul
        each article, slug in public.articles.psychology._data
            if article.published!==false
                a(href="/articles/cs/#{ slug }") #{ article.title }
                span.date   | #{ article.date }
                p(style="font-style: italic") #{ article.description }
            else
                p(style="text-decoration: underline") #{ article.title }  | #{ article.date }
                p(style="font-style: italic") #{ article.description }

