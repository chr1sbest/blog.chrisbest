// First Subject
h2
  strong Topics in Coding

p Assorted posts for the pragmatic programmer. Less theory and more practical applications of (mostly Python) conventions and ideas.
p
  ul
    each article, slug in public.articles.coding._data
      li
        if article.published !== false
          a(href="/articles/coding/#{ slug }") #{ article.title }
          span.date   | #{ article.date }
        else
          p(style="text-decoration: underline; margin-bottom: 0em") #{ article.title }  | #{ article.date }
        p.article-description #{ article.description }
hr

// Second Subject
h2
  strong Learning Computer Science
p Reviews and supplemental resources for courses that I have audited to learn computer science.  These resources have been tremendously helpful, mind-blowing, and 100% free. The total cost of enrollment for the courses reviewed below is typically over <span style="color: green; font-weight: 600">$5520</span>. Hopefully other motivated, frugal autodidacts can find these posts useful!

p
  ul
    each article, slug in public.articles.cs._data
      li
        if article.published !== false
          a(href="/articles/cs/#{ slug }") #{ article.title }
          span.date   | #{ article.date }
        else
          p(style="text-decoration: underline; margin-bottom: 0em") #{ article.title }  | #{ article.date }
        p.article-description #{ article.description }
hr

// Third Subject
h2 
  strong Etc.
ul
  each article, slug in public.articles.etc._data
    li
      if article.published !== false
        a(href="/articles/etc/#{ slug }") #{ article.title }
        span.date   | #{ article.date }
      else
        p(style="text-decoration: underline; margin-bottom: 0em") #{ article.title }  | #{ article.date }
      p.article-description #{ article.description }
